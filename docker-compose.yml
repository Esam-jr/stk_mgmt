services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: stk_user
      POSTGRES_PASSWORD: stk_pass
      POSTGRES_DB: stk_db
    ports:
      - "5444:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
